import Motion from "../motion";

const PlusSection = () => {
  return (
    <Motion key={"PlusSection"}>
      <div className="w-full h-full">Plus</div>
    </Motion>
  );
};

export default PlusSection;
