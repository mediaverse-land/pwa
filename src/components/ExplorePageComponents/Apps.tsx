import Motion from "../motion";

const AppsSection = () => {
  return (
    <Motion key={"AppsSection"}>
      <div className="w-full h-full">Apps</div>
    </Motion>
  );
};

export default AppsSection;
