version: '3.8'
services:
  app:
    image: ${APP_NAME:-0}_pwa
    build:
      context: ./
      dockerfile: Dockerfile
    restart: always
    volumes:
      - /app/node_modules
      - /app/.next
    ports:
      - ${APP_EXPOSED_PORT:-0}:3000
